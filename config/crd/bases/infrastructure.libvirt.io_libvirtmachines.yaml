---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: libvirtmachines.infrastructure.libvirt.io
spec:
  group: infrastructure.libvirt.io
  names:
    kind: LibvirtMachine
    listKind: LibvirtMachineList
    plural: libvirtmachines
    singular: libvirtmachine
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.labels.cluster\.x-k8s\.io/cluster-name
      name: Cluster
      type: string
    - jsonPath: .metadata.ownerReferences[?(@.kind=="Machine")].name
      name: Machine
      type: string
    - jsonPath: .status.instanceState
      name: State
      type: string
    - jsonPath: .status.ready
      name: Ready
      type: string
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: LibvirtMachine is the Schema for the libvirtmachines API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: LibvirtMachineSpec defines the desired state of LibvirtMachine
            properties:
              image:
                description: Image is the path or name of the VM base image to use.
                type: string
              memoryMB:
                format: int32
                type: integer
              numCPU:
                description: NumCPU and MemoryMB define the VM hardware specs.
                format: int32
                type: integer
              providerID:
                description: ProviderID will be the libvirt instance's ID in ProviderID
                  format (e.g., libvirt://<UUID>).
                type: string
            required:
            - image
            type: object
          status:
            description: LibvirtMachineStatus defines the observed state of LibvirtMachine
            properties:
              addresses:
                description: Addresses contains the associated addresses for the machine
                  (e.g., IPs).
                items:
                  description: MachineAddress contains information for the node's
                    address.
                  properties:
                    address:
                      description: address is the machine address.
                      maxLength: 256
                      minLength: 1
                      type: string
                    type:
                      description: type is the machine address type, one of Hostname,
                        ExternalIP, InternalIP, ExternalDNS or InternalDNS.
                      enum:
                      - Hostname
                      - ExternalIP
                      - InternalIP
                      - ExternalDNS
                      - InternalDNS
                      type: string
                  required:
                  - address
                  - type
                  type: object
                type: array
              ready:
                description: Ready indicates the VM has been created and is operational.
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
