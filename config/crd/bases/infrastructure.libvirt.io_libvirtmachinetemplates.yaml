---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: libvirtmachinetemplates.infrastructure.libvirt.io
spec:
  group: infrastructure.libvirt.io
  names:
    categories:
    - cluster-api
    kind: LibvirtMachineTemplate
    listKind: LibvirtMachineTemplateList
    plural: libvirtmachinetemplates
    singular: libvirtmachinetemplate
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              template:
                description: LibvirtMachineTemplateResource defines the spec for the
                  MachineTemplate's Machine.
                properties:
                  metadata:
                    type: object
                  spec:
                    description: LibvirtMachineSpec defines the desired state of LibvirtMachine
                    properties:
                      image:
                        description: Image is the path or name of the VM base image
                          to use.
                        type: string
                      memoryMB:
                        format: int32
                        type: integer
                      numCPU:
                        description: NumCPU and MemoryMB define the VM hardware specs.
                        format: int32
                        type: integer
                      providerID:
                        description: ProviderID will be the libvirt instance's ID
                          in ProviderID format (e.g., libvirt://<UUID>).
                        type: string
                    required:
                    - image
                    type: object
                type: object
            required:
            - template
            type: object
        type: object
    served: true
    storage: true
